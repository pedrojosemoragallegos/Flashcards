/*import Foundation
 
 class DeckFlashcardsViewModel<DeckType: DeckProtocol>: ObservableObject {
 @Published var specialCards: [DeckType.SpecialCardType]
 let deck: DeckType
 private let deckService: GenericDeckService<DeckType, InMemoryDeckRepository<DeckType, DeckType.SpecialCardType>>
 
 init(deckService: GenericDeckService<DeckType, InMemoryDeckRepository<DeckType, DeckType.SpecialCardType>>, deck: DeckType) {
 self.deckService = deckService
 self.deck = deck
 self.specialCards = deck.specialCards
 }
 }
 */
